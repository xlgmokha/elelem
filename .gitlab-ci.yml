default:
  image: ruby:3.4.4

  before_script:
    - apt-get update && apt-get install -y clang
    - gem update --system '3.6.9'
    - gem install bundler -v 2.6.9
    - bundle install

test:
  script:
    - ./bin/test

lint:
  script:
    - ./bin/lint
